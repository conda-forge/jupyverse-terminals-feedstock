schema_version: 1

context:
  version: "0.1.2"

package:
  name: jupyverse-terminals
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/j/jupyverse-terminals/jupyverse_terminals-${{ version }}.tar.gz
    sha256: 23905d17abc87dadec4da18092e1d76cc38533cbe546fd2f400f5ff394ae94b7

build:
  noarch: python
  number: 0
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - uv-build >=0.11.32,<0.12
  run:
    - python >=${{ python_min }}
    - fastapi-core
    - pydantic
    - jupyverse-api >=0.15.0,<0.16.0
    - jupyverse-auth >=0.2.0,<0.3.0

tests:
  - python:
      imports: jupyverse_terminals
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  license: BSD-3-Clause
  license_file: COPYING.md
  homepage: https://github.com/jupyter-server/jupyverse/tree/main/api/terminals
  summary: The API for Jupyverse's terminals.

extra:
  recipe-maintainers:
    - davidbrochart
